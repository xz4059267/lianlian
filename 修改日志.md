# 恋恋错题本修改日志

> 排查同类问题前，先搜索本文件中的“问题表现”和“根因”。记录时间均为北京时间。

## 2026-08-10

### 问题表现

- 开始讲解后的第一句声音像浏览器自带语音，后续恋恋回复才变成阿里云声音。
- 首句云端 TTS 请求或首次音频播放失败时，前端会静默切换到 `speechSynthesis`，造成音色不一致。

### 根因

- 开场提示在进入讲解页后才第一次请求云端 TTS，没有在“开始讲解”的用户操作阶段预加载。
- `lianSpeak` 的云端播放失败分支默认调用浏览器语音兜底，用户听不到失败过程，只能感知到声音突然变化。

### 修改

- 点击“开始讲解”时立即预请求第一句开场提示的阿里云音频，进入讲解页后优先复用已加载音频。
- 云端首句请求最多重试一次，应对首次令牌建立和瞬时网络抖动。
- 默认关闭浏览器自带语音兜底；云端失败时保留文字并显示“云端语音暂时不可用”，不再混用两种声音。
- 保留显式 `allowBrowserFallback` 开关，只有调用方明确允许时才使用浏览器语音。

### 验证

- `npm.cmd run check` 通过。
- `npm.cmd test` 通过：61 项通过，1 项历史测试跳过。
- `git diff --check` 通过。

## 2026-08-06

### 问题表现

- 选择题的标准答案模型已经判断出正确选项，但恋恋只收到孤立的选项字母，例如 `C`。
- 恋恋没有拿到 `C` 对应的完整选项文字，可能重新猜测选项含义，出现“推理正确但选项映射错误”的引导。
- 板书识别前还会串行调用第二次答案复核，导致首次讲解和停笔反馈变慢。

### 根因

- 答案基线只传递了 `canonicalAnswer` 和 `acceptedAnswers`，没有把选项文字、结论 I/II 的判断和字母映射作为同一个结构传递给引导模型。
- 第二次复核模型虽然也返回答案，但旧链路会再次解释选项字母，反而可能把同一个字母映射到错误含义。

### 修改

- 标准答案模型一次看图时强制返回 `choiceAnalysis`：全部选项文字、各结论的 `true/false`、`selectedOption` 和 `selectedOptionText`。
- 服务端将完整 `choiceAnalysis` 放入 `verifiedAnswerReference`，同时传给恋恋引导、最终答案核对和板书识别。
- 选择题只有在选项文字和选项映射完整时才建立可信答案基线；恋恋不得重新猜测 C、D 等字母含义。
- 移除答案基线的串行第二次复核调用，改为单次结构化标准答案调用，减少一次多模态网络请求。
- 保留前端对旧状态 `double-verified` 的兼容，并新增 `structured-single-pass` 状态，避免影响板书完成和保存错题判断。
- 答案缓存版本升级为 `v9-choice-semantics`，防止旧的错误选项映射继续命中缓存。

### 验证

- `npm.cmd run check` 通过。
- `npm.cmd test` 通过：51 项通过，1 项历史测试跳过。
- 新增回归测试覆盖：同一个选项字母但对应完整选项文字不同，必须判定为语义不一致。

## 2026-08-02 16:29:11

### 问题表现

- 上传试卷后，首页显示“AI 没有可靠拆出单题”，总耗时为 `0.0 秒`。
- 之前保存的版本可以使用阿里云切题，重新打开项目后却像没有结果。
- 用户要求继续保持“只使用阿里云切题，不启用其他兜底”。

### 根因

- 不是题目分割规则失效，也不是阿里云教育版试卷切题接口关闭。
- 本地 Node 服务启动时没有获得访问外部网络的权限，服务无法连接阿里云接口。
- 前端只能拿到接口失败结果，因此显示“没有可靠拆出单题”；由于请求在很早阶段失败，总耗时接近 `0.0 秒`。

### 修改与处理

- 保持 `.env` 中 `SEGMENT_ALIYUN_ONLY="1"`，不关闭阿里云专用模式，也不打开其他分割兜底。
- 保留阿里云教育版试卷切题接口：
  `https://subject2.market.alicloudapi.com/educationservice/papercut`
- 重启本地服务时确保服务进程具备外部网络访问权限。
- 保留前端资源缓存版本号，避免浏览器继续使用旧的切题代码。
- 保留接口耗时日志和健康检查，便于区分“接口没返回”和“算法返回空结果”。

### 验证结果

- `/api/health` 返回 `ok: true`，并确认 `segmentAliyunOnly: true`。
- 使用 `/api/segment` 实测返回 HTTP `200`。
- 返回提供方为 `aliyun-education-paper-cut-only`。
- 返回模型为 `Aliyun RecognizeEduPaperCut`。
- 实测阿里云请求耗时约 `2259 ms`，后端总耗时约 `2273 ms`。
- `server.js`、`app.js` 语法检查通过。

### 下次遇到相同问题先检查

1. 打开 `http://127.0.0.1:4173/api/health`，确认 `ok: true` 和 `segmentAliyunOnly: true`。
2. 查看页面显示的总耗时：若为 `0.0 秒`，优先检查服务是否能访问阿里云，不要先改分割算法。
3. 检查服务端是否出现 `fetch failed`、超时、DNS 或网络权限错误。
4. 确认 `.env` 中阿里云配置仍存在，且 `SEGMENT_ALIYUN_ONLY="1"` 没有被改成 `0`。
5. 用同一张图片重新请求 `/api/segment`，确认返回的 `provider` 是 `aliyun-education-paper-cut-only`。
6. 只有接口返回成功但题块不准确时，才继续排查题号锚点、边界修正和前端裁剪逻辑。

## 2026-08-02 16:59:10

### 问题表现

- 讲解页面能显示恋恋文字，但没有听到对应语音。
- 页面初次进入讲解页时尤其容易出现这个问题。

### 根因

- `/api/tts` 实际已经正常返回阿里云音频：HTTP `200`、`audio/mpeg`、提供方 `aliyun-nls`、音色 `zhimi_emo`。
- 前端在异步等待 TTS 请求完成后才新建 `Audio` 并调用 `play()`，浏览器会把这次播放视为自动播放，可能被浏览器拦截。
- 云端音频播放失败后，原逻辑只静默切换到浏览器语音；浏览器语音也可能没有播放，于是最终只看到了文字。

### 修改与处理

- 新增可复用的恋恋音频元素，不再每次创建新的 `Audio`。
- 用户点击页面时同步解锁音频播放权限，开始讲解按钮也会主动触发解锁。
- TTS 请求完成后复用已经解锁的音频元素播放阿里云音频。
- 保留“音频开始播放后才显示恋恋文字”的同步逻辑。
- 增加云端音频播放失败日志，后续可以明确区分接口失败和浏览器播放失败。
- 更新前端资源版本号为 `20260802-tts-unlock`，避免浏览器继续使用旧脚本。

### 验证结果

- `/api/tts` 实测返回 HTTP `200`。
- 返回类型为 `audio/mpeg`，提供方为 `aliyun-nls`，音色为 `zhimi_emo`。
- `app.js`、`server.js` 语法检查通过。
- 浏览器重新加载页面后无错误或警告日志。

### 下次遇到相同问题先检查

1. 先检查 `/api/tts` 是否返回 `200`、`audio/mpeg` 和 `aliyun-nls`。
2. 如果接口成功但无声，检查浏览器控制台是否有 `[tts] cloud audio playback failed`。
3. 确认页面已刷新到 `app.js?v=20260802-tts-unlock`，不要使用旧缓存脚本。

## 日志记录模板

### YYYY-MM-DD HH:mm:ss

#### 问题表现

- 

#### 根因

- 

#### 修改与处理

- 

#### 验证结果

- 

#### 下次先检查

- 

## 2026-08-03 讲解前标准答案解析改造

### 问题表现

- 进入讲解页后，恋恋有时没有基于题图的正确答案引导，容易把旧题思路或不完整答案带入当前题。
- Qwen 返回自然语言或非固定结构时，前端会显示“没有返回可解析的结构化结果”。

### 根因

- 标准答案接口虽然会调用 Qwen，但提示词对“固定 JSON 字段”和“区分题目原文/学生痕迹”的约束不够硬。
- 点击“开始讲解”后，当前题的答案解析只是进入后台预取队列，不能保证第一道题马上优先请求。

### 修改与处理

- `server.js` 新增 `STRICT_ANSWER_KEY_SOLVER_PROMPT`，要求 Qwen 只返回 schema JSON。
- 标准答案 schema 增加 `finalAnswer`、`solutionSteps`、`studentTrace`、`verification`、`knowledge`。
- 服务端将 `finalAnswer / solutionSteps / verification.checks` 统一映射回现有代码使用的 `canonicalAnswer / solutionOutline / verificationChecks`。
- 标准答案缓存版本升级为 `answer-key:v7-strict-json`，避免继续命中旧答案。
- `app.js` 改为点击“开始讲解”后，第一道题立即调用 `/api/answer-key`，其余题目再进入后台队列。

### 验证结果

- `node --check server.js` 通过。
- `node --check app.js` 通过。

### 下次先检查

- 看 `/api/answer-key` 是否返回 `ready=true`、`canonicalAnswer`、`solutionOutline`。
- 如果提示“没有返回可解析的结构化结果”，优先检查 Qwen 返回是否符合 schema，而不是先改前端判断。
- 如果恋恋引导旧题内容，先确认当前题是否已经命中 `answer-key:v7-strict-json` 后的新缓存。
## 2026-08-03 18:12:00 阿里云切题 0.1 秒返回空结果

### 问题表现

- 上传图片后右侧显示“AI 没有可靠拆出单题，请改用手动框选”，总耗时约 `0.1 秒`。
- 这类耗时太短，不像阿里云教育版试卷切题接口真实完成，更像服务端请求在早期失败或结果说明被前端覆盖。

### 根因

- 修改日志中已有同类记录：`0.0/0.1 秒` 失败时，优先检查本地服务进程是否具备外部网络访问权限，不要先改切题算法。
- 前端原本没有展示后端返回的 `note` 或接口错误说明，导致真实原因被笼统的“AI 没有可靠拆出单题”覆盖。

### 修改与处理

- 保持 `.env` 中 `SEGMENT_ALIYUN_ONLY="1"`，继续只走阿里云教育版试卷切题。
- 按日志要求重启本地服务，并确保服务进程具备访问阿里云 OCR API 的网络权限。
- 修改 `app.js`：当 `/api/segment` 返回 0 个题块时，优先展示后端 `note` 或接口错误信息。
- 更新 `index.html` 前端脚本版本为 `20260803-segment-note-visible`，避免浏览器继续使用旧提示逻辑。

### 验证结果

- `/api/health` 返回 `ok: true`。
- `segmentAliyunOnly: true` 保持不变。
- `aliyunOfficialEduPaperCutConfigured: true`。
- `node --check app.js` 通过。
- `node --check server.js` 通过。

### 下次先检查

1. 如果耗时仍是 `0.0/0.1 秒`，先看页面展示的后端 `note`。
2. 打开 `/api/health`，确认 `segmentAliyunOnly: true` 和阿里云配置为 true。
3. 如提示网络/API 错误，优先重启服务并确保服务进程有外部网络权限。
4. 只有接口真实返回题块但裁剪不准时，才继续查题块过滤、坐标映射和前端裁剪逻辑。

## 2026-08-04 14:01:41 讲解页单题本地校验误导

### 问题表现

- 讲解页在判断类、结论类或选项类题目中，把学生用于反驳某个结论的中间结果误说成“不符合题目条件”。
- 之前为了修一张箭头关系题，代码里残留了只针对该题型的本地确定性校验，无法覆盖其他同类题，且容易造成维护误判。

### 根因

- 板书识别链路把“原题条件”和“正在被检验的结论/选项”混在一起判断。
- 当学生推导出的结果与某个待判断结论矛盾时，模型可能把这个矛盾当作学生算错，而不是把它理解为有效反例。
- 单题本地校验没有共性价值，不能作为恋恋判断答案正确性的长期方案。

### 修改与处理

- 禁用 `deterministicArrowDifferenceAnswerKey` 和 `deterministicArrowHandwritingAudit` 的运行效果，不再用某一道题的本地特判覆盖模型判断。
- 新增 `STATEMENT_EVALUATION_RULES`，统一要求讲解、板书识别、板书审稿和讲解审稿区分“原题条件”和“被检验的结论/选项”。
- 新增 `applyStatementEvaluationSafety`：如果是结论/选项核对题，且模型只给出笼统的“与题目条件不符”，但没有指出明确运算、符号、代入或列式错误，则降级为不确定，不播出误导性错误判断。
- 新增回归测试，覆盖“反例推导不能被笼统判错”的共性场景。

### 验证结果

- `npm.cmd run check` 通过。
- `npm.cmd test` 通过：45 个通过，1 个旧的单题本地答案覆盖测试被明确跳过。

### 下次遇到相同问题先检查

1. 先查 `server.js` 中是否又新增了 `deterministic...`、具体题号、具体题型截图坐标等单题特判。
2. 对判断/选项/结论题，先确认标准答案是否来自题图多模态求解与审稿，而不是旧缓存或本地硬编码。
3. 如果恋恋说“与题目条件不符”，必须检查它是否指出了具体原题条件或具体运算错误；如果只是笼统不符，应走 `applyStatementEvaluationSafety` 降级，不直接播出。
4. 不要为了某张截图新增单题规则，应抽象为：原题条件、待检验结论、学生反例/选项核对、最终选项判断这四类信息的通用处理。

## 2026-08-05：加快板书识别与恋恋回复

### 问题

- 板书识别请求上传的图片过大，网络传输和模型视觉处理耗时较长。
- 普通停笔识别后，即使高置信度结果已经明确，也会再次调用模型核验，导致恋恋回复变慢。

### 修改

- 板书专用截图由最大宽度 1280px、PNG 改为最大宽度 1024px、JPEG 质量 0.84，减少请求体积。
- 普通板书识别只上传题目图片和纯板书截图；只有“完成讲解前检查”才额外上传合成板书图，避免每次识别重复生成和上传大图。
- 高置信度且判断为正确的普通板书结果跳过二次核验；错误结果、低置信度结果、结论/选项判断题和完成讲解前检查仍保留核验，避免牺牲正确性。
- 后端新增板书识别耗时日志，记录标准答案获取、首次模型识别和总耗时，并将 `timingMs` 返回前端，便于定位慢在哪一步。

### 验证

- 保留板书识别 6.5 秒停笔触发阈值不变。
- 普通高置信度正确结果减少一次模型请求；首次没有标准答案缓存时，仍会等待标准答案生成，后续请求可复用缓存。
- 修改后继续执行 `npm.cmd run check` 和 `npm.cmd test`。

## 2026-08-05：修复引导与当前板书不一致

### 问题

- 恋恋引导请求没有携带最近一次结构化板书识别结果，模型可能按旧板书、旧语音或题目上下文自行补全。
- 用户刚写完板书时，6.5 秒识别请求可能尚未返回，但引导已经先触发，容易把旧状态当成当前状态。

### 修改

- 前端记录 lastHandwritingRecognizedAt，讲解请求新增 latestHandwritingResult 和 boardPendingRecognition。
- 服务端新增通用板书一致性规则：wrong 优先依据具体纠错和下一步引导，correct 禁止反向判错，unclear/incomplete 禁止下确定结论，识别尚未更新时先等待结果。
- 新增回归测试，覆盖错误板书、正确板书和识别待更新三种情况。

### 验证

- 运行 npm.cmd run check
- 运行 npm.cmd test

## 2026-08-05：修复开场文字先于语音出现

### 问题

- 点击开始讲解后，开场提示先被直接写入文字框；随后 `lianSpeak` 又生成另一条提示并播放，导致第一条只有文字没有声音。

### 修改

- 删除开始讲解流程中的直接文字发布。
- 开场文字统一由 `lianSpeak` 在云端音频开始播放或浏览器语音开始播放时发布。
- 如果语音服务失败，仍在最终回退时显示对应提示，避免文字和语音内容不一致。

### 验证

- `npm.cmd run check`
- 手动检查开始讲解时文字与当前播放语音使用同一条提示。

## 2026-08-05：调慢阿里云 TTS 语速

### 问题

- 阿里云语音合成使用默认 `speech_rate=0`，恋恋播报速度偏快。

### 修改

- 阿里云 NLS TTS 默认 `speech_rate` 调整为 `-30`，负值表示放慢。
- 保留 `ALIYUN_NLS_TTS_SPEECH_RATE` 环境变量覆盖能力，部署环境可继续自行微调。
- 在 `.env.example` 增加该配置示例。

### 验证

- `npm.cmd run check`
- `git diff --check`

## 2026-08-06：修复保存后切换下一题的重复提示

### 问题

- 保存错题后切换下一题时，重置流程和下一题流程各自触发一次恋恋提示。
- 两次提示互相取消或排队，造成文字先出现、语音随后才播放，切题体验变慢。

### 修改

- `goToLectureQuestion` 传入下一题开场提示时，禁止重置流程再次播报换题提示。
- 切题前统一停止上一题的云端音频和浏览器语音，下一题只创建一次 `lianSpeak` 请求。
- 下一题语音开始前不再残留上一条提示文字，避免文字与语音错位。

### 验证

- `npm.cmd run check`
- `npm.cmd test`

## 2026-08-10：修复最后一步板书未进入答案核对

### 问题

- 板书识别结果返回后，主流程没有调用已有的 `maybeVerifyFinalAnswerFromHandwriting`，因此最后一步只能显示“板书已记录”，不会立即核对答案。
- 板书答案候选提取混入了模型内部的 `calculationCheck`、`issueSummary` 和 `positiveFeedback`，可能把模型解释当成学生实际写出的答案。

### 修改

- 将最终答案判断接入 `runHandwritingRecognition`，在“板书已记录”之前优先进入“我来核对答案”。
- 兼容 `mathExpression`、`detectedWriting`、`finalAnswer`、`answer`、`studentAnswer`、`recognizedText`、`boardText` 和 `completedSteps` 等可见结果字段。
- 只有出现最终性信号、完整状态、可核验关键步骤或明确答案表达时才触发核对，避免中间步骤误触发。
- 答案候选不再读取模型内部核验说明，只依据可见板书证据。
- 增加板书决策日志，记录题目、请求、板书版本、完成步骤和最终答案候选，便于排查“识别到了但没有核对”。
- 更新板书识别提示词，要求最终结果必须写入可见板书转写字段。

### 验证

- `npm.cmd run check`
- `npm.cmd test`
- `git diff --check`

## 2026-08-09：区分不同板书识别失败并修复旧请求误报

### 问题

- 第一次识别失败后，学生继续写板书；第二次停笔再次识别也失败。前端原先只显示相同的“板书识别失败”，无法判断是快照为空、模型没有返回结构化 JSON，还是 Qwen 接口网络/鉴权异常。
- 旧请求在学生已经继续写字后才返回失败时，可能覆盖当前板书状态，造成误导。

### 修改

- 为每道题维护板书版本号；每次开始写板书都会递增版本，识别请求记录 `requestId`、`questionId`、`boardVersion`、触发原因、画布尺寸和快照大小。
- 返回结果和失败结果都必须匹配当前题目、当前交互会话和当前板书版本；旧版本结果静默丢弃，不再更新提示或触发语音。
- 同一道题在识别请求进行中再次停笔时，记录排队原因，当前请求结束后重新识别最新板书，避免两次不同板书被混为一次请求。
- 后端区分并记录快照为空、Qwen 网络/服务错误、模型非结构化返回等阶段，并把失败阶段返回给前端。
- Qwen 非 JSON 返回时记录响应字段、完成原因和文本长度，便于判断“模型未回答”还是“代码无法解析”。

### 验证

- `npm.cmd run check`
- `npm.cmd test`
- 新增独立板书识别请求版本回归测试。

## 2026-08-07：修复语音识别文字不同步

### 问题

- 阿里云 ASR 采用分段上传，浏览器识别结果只作为临时预览；当云端返回较慢、空结果或请求失败时，临时文字会在停止收听或识别重启时被清掉，导致用户看到文字短暂出现后没有正式同步。
- 停止收听时浏览器识别器的 `onend` 可能先于最后一批云端音频返回，提前消费或清除语音草稿。
- 讲解文本只更新了输入框，没有同步保存到当前题目的内存状态，切题时可能丢失最新内容。

### 修改

- 保留浏览器实时预览中的最终识别片段，并将其与临时片段一起显示，减少文字跳变。
- 云端 ASR 返回成功时使用云端结果；返回空结果、请求失败或停止收听时，自动提交浏览器已确认的文字作为回退，避免语音内容丢失。
- 停止收听后增加整理状态，等待最后一批云端结果或本地回退完成，避免 `onend` 抢先清除草稿。
- 讲解文本的新增、预览和清理同步写入当前题目的 `transcriptsByQuestion`。

### 验证

- `npm.cmd run check`
- `npm.cmd test`（51 passed，1 skipped，0 failed）

## 2026-08-05：板书优先于语音核对

### 问题

- 学生只说出数学答案、但没有留下板书时，语音不应直接被当作非选择题的计算依据。
- 语音和板书不一致时，应优先依据最新板书状态反馈。

### 修改

- 讲解请求增加 `questionType` 和 `hasBoardInk`，传给 Qwen 引导层。
- 非选择题没有板书且语音包含数学内容时，先提醒学生把关键式子或计算过程写在黑板上。
- 选择题也必须先留下板书；学生口述选项只能帮助理解意图，不能替代板书核对。
- 有板书时以最新板书识别结果为数学判断依据，语音只用于理解学生意图。
- 增加无板书提醒与选择题语音答案例外的回归测试。

### 验证

- `npm.cmd run check`
- `npm.cmd test`

## 2026-08-06：统一语音识别与讲解文本的提交顺序

### 问题

- 阿里云 ASR 返回文字后，还要等待文本纠错和恋恋判断，导致讲解文本、语音回复与用户实际说话出现不同步。
- 浏览器实时预览、阿里云最终识别和恋恋回复分别更新，用户继续说话时旧语音还可能继续播放。
- 停止收听时，如果上一段 ASR 请求仍在进行，新增录音片段可能没有继续提交。

### 修改

- 阿里云 ASR 结果返回后，先立即写入讲解文本框并记录真实识别时间，再排队进行数学纠错和恋恋判断。
- 新增识别处理队列，保证多段语音按返回顺序处理，避免纠错请求互相覆盖最后一段文字。
- 浏览器实时预览检测到用户继续说话时立即打断旧的恋恋语音，云端结果作为最终文字来源。
- 增加重复云端片段过滤和请求序号校验，避免重复显示或旧请求覆盖新内容。
- 收听结束时保留在途请求之后的录音缓存，继续完成最后一段识别。

### 验证

- `npm.cmd run check`
- `npm.cmd test`（50 passed，1 skipped，0 failed）
- `git diff --check`

## 2026-08-05：修复语音识别没有实时显示

### 问题

- 启用阿里云 ASR 时，前端原先每约 2.8 秒才上传一段录音，期间讲解文本框没有实时内容。

### 修改

- 增加浏览器语音识别的实时预览层，只显示当前临时文字，不把浏览器临时结果当作最终答案，避免与阿里云结果重复。
- 阿里云 ASR 仍作为最终文本来源，分段上传间隔缩短为约 1.6 秒。
- 停止收听时同时停止实时预览，避免旧临时文字残留。
- 浏览器不支持语音识别时，保留原有阿里云分段识别流程。

## 2026-08-06：板书识别后的过期反馈静默
### 问题

- 板书识别或引导请求返回较慢时，学生已经开始写新的板书或说出新的思路，旧识别结果仍可能触发恋恋语音和文字回复，打断学生。

### 修改

- 为板书识别和引导请求记录板书、已识别语音的时间快照。
- 识别结果返回后，如果快照之后出现新笔迹或新语音，直接丢弃这次旧反馈，不调用语音播报。
- 板书核对、正确反馈、错误引导、保存提示和延迟保存回调在播报前再次检查输入新鲜度。
- 将麦克风持续采样与真正识别出的语音分开，避免静音采样误判为新语音。

### 验证

- `npm.cmd run check`
- `npm.cmd test`
