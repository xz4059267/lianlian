# 恋恋错题本工作日志

## Vercel 部署记录

### 项目关联

- Vercel 项目：`lianlian1/lianlian`
- GitHub 仓库：`https://github.com/xz4059267/lianlian.git`
- 当前代码分支：`master`
- Vercel 项目已关联 GitHub，推送到 `master` 后会触发新的部署流程。
- 项目目录中存在 `.vercel/project.json`，用于保存 Vercel 项目关联信息。

### 2026-08-10：修复最后一步板书核对后的预览部署

#### 修改内容

- 修复板书识别结果返回后没有进入最终答案核对的问题。
- 最终板书答案优先进入“我来核对答案”，不再直接显示“板书已记录”。
- 答案候选只从可见板书字段和已完成步骤中提取，避免把模型内部说明误当成学生答案。
- 增加板书决策日志，便于排查识别结果、板书版本和答案候选。
- 更新了板书识别提示词和回归测试。

#### 提交记录

- Git 提交：`8cb84ea 修复最后一步板书答案核对`
- 已推送到 GitHub：`master -> master`
- 提交前验证：`npm.cmd run check` 通过，测试 `61 passed，1 skipped，0 failed`。

#### Vercel 预览部署

- 部署命令：`vercel.cmd --yes`
- Vercel CLI：`58.5.1`
- 部署 ID：`dpl_FZFMdARBiRNY1Up4kCq5brmBkXgi`
- 预览地址：`https://lianlian-d18twso0r-lianlian1.vercel.app`
- 检查地址：`https://vercel.com/lianlian1/lianlian/FZFMdARBiRNY1Up4kCq5brmBkXgi`
- 构建结果：`READY`
- 构建过程：依赖安装和 `vercel build` 均成功。
- 访问检查：预览地址返回 HTTP `200`。
- 注意：预览环境启用了 Vercel Deployment Protection，未登录时会看到 `Login - Vercel` 页面，这不是应用构建失败。
- 本次只部署了 Preview，没有执行 `vercel deploy --prod`，未修改生产环境。

### 之前的预览部署

- 部署 ID：`dpl_9u3ksowCFCQXgsQRCkdFobLTVX78`
- 预览地址：`https://lianlian-fi8q9jpwy-lianlian1.vercel.app`
- 状态：`READY`
- 用于验证板书识别诊断、请求版本控制和旧结果丢弃逻辑。

## Vercel 环境变量记录

`.env` 只用于本地开发，Vercel 不会自动读取本地 `.env`。需要在 Vercel 项目设置中分别添加到 `Preview` 和 `Production` 环境。日志只记录变量名，不记录密钥值。

### AI、OCR 与语音相关变量

- Qwen 多模态模型和 API 配置变量
- 阿里云教育版试卷切题 OCR 配置变量：`ALIYUN_OCR_APPCODE`、`ALIYUN_OCR_URL`
- 阿里云 OCR 鉴权变量：`ALIYUN_OCR_ACCESS_KEY_ID`、`ALIYUN_OCR_ACCESS_KEY_SECRET`
- 阿里云语音识别和语音合成项目配置变量
- 当前项目曾切换过 Qwen 模型，修改模型后必须重新部署 Preview 才会生效。

### Supabase 云端错题本

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_NOTEBOOK_TABLE`
- Service Role Key 只能配置在 Vercel 服务端环境变量中，不能写入前端代码、日志或公开仓库。

## 部署时遇到的问题

### PowerShell 无法运行 `vercel`

PowerShell 曾提示禁止运行：

```text
vercel.ps1 because running scripts is disabled
```

解决方式：使用 Windows 命令入口：

```powershell
vercel.cmd --version
vercel.cmd --yes
```

### Git 提交权限问题

项目代码可以修改，但普通权限下曾无法创建 `.git/index.lock`。使用受控权限完成了 Git 暂存、提交和推送。若再次出现相同问题，先确认没有残留的 `.git/index.lock`，再用有权限的 Git 操作完成提交。

## 下次部署流程

1. 修改代码后执行：

```powershell
npm.cmd run check
npm.cmd test
git diff --check
```

2. 确认 `.env` 没有被提交，并检查 Vercel Preview 环境变量是否完整。

3. 提交并推送：

```powershell
git add <修改文件>
git commit -m "说明本次修改"
git push origin master
```

4. 手动发布 Preview 时执行：

```powershell
vercel.cmd --yes
```

5. 记录部署 ID、Preview 地址和 `READY` 状态。

6. 登录预览地址后验证关键流程：上传题目、AI 分割、进入讲解、板书识别、语音输出、保存错题本和 Supabase 同步。

7. 只有确认 Preview 正常后，才执行生产发布：

```powershell
vercel.cmd deploy --prod
```

## 2026-08-10：沉默引导具体式修复

- 根因：服务端安全审查会清空不可信模型算式，前端初始 `silence` 事件也漏出具体步骤拼接。
- 处理：从 Question Memory 恢复题目专属下一步；阶段 1～3 分别输出关系、运算关系和关键式；板书待识别时继续等待，不覆盖新板书状态。
- 验证：`npm.cmd run check` 通过；`npm.cmd test` 通过（64 passed，1 skipped，0 failed）。
